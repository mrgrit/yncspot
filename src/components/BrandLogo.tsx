import { Rocket, type LucideIcon } from "lucide-react";

/**
 * 시스템 메인(브랜드) 아이콘 — 여기 한 줄만 바꾸면
 * 헤더(Topbar)·로그인·랜딩 등 브랜드 마크 전체에 반영됩니다.
 * (예: Rocket → Zap / TrendingUp / Sprout 등 lucide 아이콘으로 교체)
 */
export const BrandIcon: LucideIcon = Rocket;
